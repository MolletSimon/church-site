import React from "react";
import './index.scss';

const Divider = ({width}) => {
    return(
        <div className="divider" style={{width: width}}>

        </div>
    )
}

export default Divider;
